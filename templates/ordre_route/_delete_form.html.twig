<form method="post" action="{{ path('ordre_route_delete', {'id': ordre_route.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordre_route.id) }}">
    <button class="btn">Delete</button>
</form>
