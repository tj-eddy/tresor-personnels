{% extends "base.html.twig" %}

{% set fs_link = true %}

{% block body %}
    {% include 'partials/header.html.twig' with
        {
            bc_title : 'Modification une OR'|trans,
            path_link : path('ordre_route_index') ,
            title_link : 'return'|trans ,
        } %}
    <!-- Page content -->
    <!-- Page content -->
    <div class="container-fluid mt--6">
        <div class="row">
            <div class="col">
                <div class="card">
                    <!-- Card header -->
                    <div class="card-header border-0">
                        {{ include('ordre_route/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/ordre.route.js') }}"></script>
{% endblock %}
