{% extends 'base.html.twig' %}

{% block title %}OrdreRoute{% endblock %}

{% block body %}
    {% include 'partials/header.html.twig' with
        {
            bc_title : 'Enregistrement ordre de route'|trans,
        } %}
    <!-- Page content -->
    <div class="container-fluid mt--6">
        <div class="row">
            <div class="col">
                <div class="card">
                    <!-- Card header -->
                    <div class="card-header border-0">
                        <a class="allow-pdf-download" href="{{ path('generate_or_pdf',{'id': app.user.id}) }}"><img
                                    width="100px"
                                    height="100px"
                                    src="{{ asset('/img/logo_pdf.png') }}"
                                    alt="télécharger"></a>
                        {% include 'ordre_route/template_or.html.twig' %}
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
